# Progress — Beyond Folio

> The decisions log + open questions + status tracker.
> Update this file every meaningful decision or completed task.

---

## What Works (Completed)

### Pre-implementation doc reconciliation (blockers cleared before E1)

- ✅ **Pre-EPIC blocker reconciliation (2026-09-22).** Resolved the two real pre-implementation blockers plus a provider decision and two clarifications, applied across `DYNAMODB_DATA_MODEL.md`, `TRD.md`, `PRD.md`, `PHASE_1_BACKLOG.md`, `docs/SETUP_PREREQUISITES.md`, and the reference memory-bank files (`systemPatterns.md`, `techContext.md`):
  1. **Price-cache key `exchange` → `currency`.** Items now keyed `PRICE#<canonicalSymbol>#<currency>` / `PRICEHIST#<canonicalSymbol>#<currency>` (data model §9.2/§9.6, all AP tables + examples + assumption A-7; TRD §6.1–§6.3 interface `getCurrentPrice(canonicalSymbol, currency)` / `getDailySeries(...)` routed **by currency**; backlog E3/E10/E11). Rationale: broker files reliably carry account currency but **not** exchange (only Zerodha names it), so currency is the only always-populatable disambiguator and it is the routing axis. **Trade item unchanged** — the earlier "exchange gap" is dissolved, not patched. Accepted limitation: NSE vs BSE (both INR) share one cache entry (new data model **OQ-E2**) — immaterial to XIRR.
  2. **Table count → 4** — `BeyondFolio` + Price-cache + Sessions + **Waitlist**, the Waitlist marked **temporary launch-gating** and retired at full launch (new data model **§9.8**; TRD §7.1; backlog E2 scope note; data model §6 prose/callouts/forward-map). systemPatterns/techContext table-count notes updated.
  3. **OQ-E RESOLVED** — providers chosen: **INR → Zerodha Kite API** (current + historical; also Phase-2 login; needs paid historical add-on + user Kite session), **USD → Twelve Data** (over AlphaVantage's throttling free tier). One interface routed by currency; E11-T1 changed from selection-spike to integration-prep; Setup Tier E updated; PRD OQ-E → Resolved.
  4. **Clarifications** — (a) deferred _holdings/positions view_ vs. Phase-1 _transient derived holding quantity_ for XIRR (data model §1.5, PRD FR-X1); (b) admin provisioning = manual DynamoDB `role` edit per E13 runbook, no in-app promotion in Phase 1 (PRD FR-A acceptance criteria) — an operational step, not a design risk.
     **No open blockers remain; next actor begins at E1.**

### Phase-1 Backlog Phase (authoring `PHASE1_BACKLOG.md`)

- 🔄 **Backlog authoring underway (2026-09-16).** Building `PHASE1_BACKLOG.md` per `HANDOFF-BACKLOG.md`, epic-by-epic in **batches of 2 with a review gate**. Agreed structure: **13 epics** (refined from HANDOFF §4's 11). Task format = ID · Title · User story _(user-facing)_ / `[Enabler]` · Description · References · Dependencies · Acceptance criteria (Given/When/Then). Two changes vs. HANDOFF §4: (1) old Frontend epic **split** into **E2 Public Site & Waitlist** (moved front, ships early for traction) + **E12 Private App UI** (late); (2) **🆕 new scope** — a landing page + waitlist backed by an **in-stack DynamoDB `WAITLIST` table** + validated/rate-limited public endpoint (no third-party vendor). ✅ **Batch 1 done**: preamble + epic map + **E1 Project & Foundation (thin slice, T1–T7)** + **E2 Public Site & Waitlist (T1–T4)**. ✅ **Batch 2 done**: **E3 Data-Access Layer (T1–T5)** — CDK main table+GSIs, ElectroDB entities for all §7 item types, GSI/AP wiring, deterministic-key/idempotent-write dedup helpers (carries OQ-A), raw-SDK price-cache/sessions module; **E4 Auth & Session (T1–T8)** — sessions table + OAuth secret + asymmetric JWT keys in CDK, Google OIDC flow (`openid-client`), first-login provisioning (resolves OQ-D), hybrid token issue (`jose`), refresh/rotation, logout/revocation (FR-A3), tRPC middleware + 3 procedure types (FR-P1/ADR-002), browser token posture. **Infra provisioned just-in-time (Option B):** deferred AWS resources land in the epic that first uses them (main table+GSIs→E3-T1, sessions+SSM OAuth secret+JWT keys→E4-T1, S3+SQS/DLQ→E6/E7, price-cache→E11).
- ✅ **`docs/SETUP_PREREQUISITES.md` created (2026-09-17).** Checklist of external accounts/credentials/DNS/tooling/content needed to implement Phase 1, grouped by the epic that first needs each (Tier A local dev → B cloud foundation → C public launch → D auth → E market data [pending OQ-E] → F ops/first-admin), plus a secrets/config inventory (all SSM SecureString via `getSecret()`, never committed/logged), product-content decisions (landing copy/branding/waitlist wording), early choices to pin (package manager, test runner), and a "safe to start now" note (E1-T1→T3 need only Tier A). Reference-only — introduces no new decisions; OQ-E stays flagged. Lives in `docs/` alongside the future E13-T3 first-admin runbook.
- ✅ **Reflowed to full-width lines (2026-09-17, formatting-only).** `PHASE_1_BACKLOG.md` prose (Description, acceptance-criteria bullets, Demo, epic Goal/Outcome blockquotes) unwrapped from ~70-char hard wrapping into continuous full-width lines (3776→2222 lines); tables, headings, `---`, and blank-line separation preserved. Verified unchanged: 80 tasks, 13 epics, 577 table rows (identical to pre-reflow), 33 FRs. No content change — cosmetic only. Backup at `/tmp/PHASE_1_BACKLOG.bak.md`.
- ✅ **FULL BACKLOG COMPLETE + REVIEWED (2026-09-17).** `PHASE_1_BACKLOG.md` (repo root) now holds **all 13 epics / 80 tasks**, authored in the final format (Field table → Description → Acceptance-Criteria checkboxes → Demo). Full cross-doc consistency review passed: stack/architecture, just-in-time infra chain, auth (TRD §5), two-gate import + D1–D14 parsers, XIRR §10.2, journal/scorecard ADR-006, OQ-E as explicit spike (E11-T1). **All 33 Phase-1 FRs now referenced; AP-1…AP-34 fully covered.** Review fixes applied: (1) deleted the stale `PHASE1_BACKLOG.md` stub (kept `PHASE_1_BACKLOG.md`); (2) added **E7-T2b** — apply canonical symbol resolution during normalisation (FR-C1/AP-26, was missing the read path); (3) backfilled FR tags (FR-A4→E4-T3, FR-M1→E8-T2, FR-M2/FR-X3→E8-T4, FR-D1/D2→E3-T4 & E7-T7); (4) fixed confusing E7-T7 acceptance-criterion wording (now a partial-overlap dedup case). Backlog authoring is **DONE** — next actor can begin implementation from E1.

### TRD Phase (authoring `TRD.md`)

- ✅ **`HANDOFF-BACKLOG.md` created (2026-09-07).** Self-sufficient handoff for the next phase (Phase-1 task backlog), written while context was fresh. Structure: §0 how-to-start · §1 what Beyond Folio is · §2 status snapshot (TRD v1.0, PRD v1.5, all docs consistent) · §3 locked-decisions A-to-Z digest (do-not-relitigate) · §4 the next task (backlog goal + suggested 11-epic breakdown/build order + open items to carry) · §5 end goal & scope guardrails · §6 conventions · §7 reference pointers. `activeContext.md` Current Focus updated to point to it. **A fresh chat should read `HANDOFF-BACKLOG.md` + memory bank + the 4 source docs, then build the Phase-1 backlog (suggested output `PHASE1_BACKLOG.md`).**
- ✅ **Round-2 review polish (2026-09-07).** Full fresh cross-doc review found no high/medium issues (all Round-1 fixes held). Applied 3 low-severity fixes: **L1** — PRD §12 glossary "Broker account" clarified ("several across brokers; Phase 1 = one per broker, multi-account-per-broker is Phase 2"); **L2** — FEATURES §7 intro notes some items are now settled ("decided"/"mostly decided") so the "Open Questions" heading doesn't imply all-open; **L3** — FEATURES §5.4 Robinhood fee wording softened ("only charge is Gold" → "charges — e.g. Gold subscription, account, and regulatory fees — as their own rows"). Deferred (optional/cosmetic): L4 (historical changelog "3 API Lambdas" — correct as-is), L5 (§11 example spans 3 account types under one acc_fid — covered by the note). All four docs mutually consistent.
- ✅ **Cross-doc review fixes + polish (2026-09-07).** Fixed review findings: **H1** — PRD §10 OQ-E/OQ-F + FEATURES §7 updated from "open" → "resolved in TRD" (OQ-F=§5 hybrid; OQ-E=§6 approach+1M/6M/1Y, provider TBD); FEATURES §5.12 chart range → "selectable 1M/6M/1Y"; PRD → **v1.5**. **H2** — PRD Last-updated → 2026-09-07. **M3** — added a cross-reference-conventions note to TRD §0.4 (ADR-NNN = data model §5; bare §N = TRD, "data model §N" for the data model). **M4** — prefixed the genuinely-ambiguous data-model refs in the TRD ("data model §10.8/§9.6/§7.12/§7.3"). Also fixed two **stale** TRD spots: §5.4/§7.1 "⏳ pending reconciliation" flags flipped to "✅ done" (the sessions-table reconciliation was already applied), and §0.2 "2 DynamoDB tables" → three (sessions). All docs now mutually consistent; no stale open/pending/two-table language remains.
- ✅ **TRD §1 Architecture Overview authored — TRD structurally COMPLETE (2026-09-07).** §1 written last as pure synthesis: 1.1 component diagram (Mermaid; `auth-api` as sole edge to sessions + Google OAuth; asymmetric-key SSM edges; Direct topology; 3 DynamoDB tables) + component-responsibility table; 1.2 flows (sign-in / read / evaluate step lists + two-gate import **sequence diagram**); 1.3 trust boundaries; 1.4 cross-cutting principles. **All sections §0–§10 now authored; TRD bumped to v1.0 (draft for review).** Source docs (data model, PRD v1.4, FEATURES) fully reconciled; no pending items. **Next: full-document review / audit pass.**
- ✅ **TRD §10 Open Questions & Phase-2 Seams authored (2026-09-07).** Clean structure (no reconciliation bucket, since those are now applied): 10.1 open TRD Qs (provider selection + India coverage; per-provider current-day-bar behavior), 10.2 deferred hardening (staging / full-E2E+coverage / SLOs / Secrets Manager / 3rd-party observability), 10.3 Phase-2 seams (Zerodha+linking, multi-account Fidelity, undo-import, acct-number encryption, admin user-mgmt, websockets, chart >1Y + realized P/L), 10.4 reversibility seams, 10.5 pointer to data-model §13. TRD v0.8. **Only §1 Architecture Overview remains (written last, synthesizes all decisions + Direct topology).**
- ✅ **Source-doc reconciliation — DONE (2026-09-07).** Applied the two flagged reconciliations of locked decisions back into the source-of-truth docs. **Item 1 (sessions table):** `DYNAMODB_DATA_MODEL.md` ADR-003 extended (2nd purposeful exception), §6.1 → "three tables", §6.5 forward-map row, new §9.7 (TRD-owned), §3/§6 intros + index. **Item 2 (import pipeline):** data model §10.8 rewritten to the **two-gate async flow**, §7.3 gains `ImportedFile.status` + create-at-upload note, ADR-015 two-step wording, ADR-016 temp→promote mechanics; `activeContext.md` L4/L5/D13/D14 + DONE-banner refinement notes; **PRD** FR-I4 → two-step async flow (v1.4); **FEATURES** §5.1 plain-language two-step. TRD §2.6 flag flipped ⏳→✅. No stale old-flow language remains. **Next: TRD §10 (write clean, no 10.2), then §1 Architecture Overview (last).**
- ✅ **TRD Decision 8 — Non-Functional Requirements (§9) (2026-09-07).** **Observability = CloudWatch** (structured JSON logs + correlation id + metrics + DLQ/error-rate alarms) **+ X-Ray** (all ~free). **Upload = pre-signed S3 direct upload into `temp/`, 5 MB cap** (refines §2.3/§7.2). **PII-in-logs never-log policy** + redaction helper + named-safe-fields. **Testing = balanced tier** (required unit+integration+parser-fixtures+light E2E smoke; emphasis import/dedup/auth/XIRR); full E2E + coverage thresholds deferred. **No numeric SLAs Phase 1** (not real-time; cold-starts OK). Rest consolidated from §4–§8. Full record in `activeContext.md`. **Next: TRD §10 Open Questions & Phase-2 Seams, then §1 Architecture Overview (last).**
- ✅ **TRD Decision 7 — Deployment, Environments & CI/CD (§8) (2026-09-07).** **IaC = AWS CDK (TypeScript)**; **Next.js hosting = OpenNext + CDK** (avoid host-specific features for portability); **environments = local (DynamoDB Local + LocalStack) + CI + prod only** (no staging); **CI/CD = GitHub Actions + OIDC, fully automated CD to prod on merge, no manual gate** (safeguards: required CI gate, branch protection, CFN auto-rollback, idempotent/additive migrations, fast revert); **seed = version-controlled BrokerMapper script (idempotent on deploy)**, **first admin = one-time manual DynamoDB edit** (runbook, not pipeline). Full record in `activeContext.md`. **Next: TRD §9 Non-Functional Requirements.**
- ✅ **TRD Decision 6 — Storage & Infrastructure (§7) + Client→API topology (2026-09-07).** **Topology = DIRECT** (browser→API Gateway/tRPC directly, Bearer token; Next.js serves pages only; BFF rejected) — locked early for §7/§8, written into §1 later. **§7:** secrets in **SSM Parameter Store (SecureString)** + env-var config, via `getSecret()` helper (Secrets Manager deferred); **one S3 bucket** with `temp/` prefix + prefix-scoped lifecycle expiry (permanent keep-forever `<userId>/<contentHash>`); **PITR on main table only** (caches/sessions off); **no VPC** (identity-based security). Provisioning: on-demand DynamoDB, TTL on cache+sessions, 3 GSIs referenced from data model, S3 per ADR-016, SQS+DLQ. Sessions table still ⏳ ADR-003 reconciliation. Full record in `activeContext.md`. **Next: TRD §8 Deployment, Environments & CI/CD.**
- ✅ **TRD Decision 5 — External Integrations (§6) / OQ-E resolved (2026-09-07).** Market-data via a **provider abstraction** (`getCurrentPrice`/`getDailySeries`); **provider count (1 vs 2) deferred** (abstraction makes it ~1/10 to change). **OQ-E resolved:** selectable **1M/6M/1Y** daily, >1Y Phase 2, **cache 1Y once & slice**. **New last-completed-session rule** (drop provisional current-day bar, label "as of <date>"; `fetchedDate`=re-fetch timing only). Freshness windows = app-config (current-price ~15min default). Graceful degradation on provider failure (serve stale/last-cached + notice). Google OAuth cross-ref §5; Zerodha Phase-2 seam. Open sub-Qs → §10: provider selection + India coverage; per-provider current-day-bar behavior. Full record in `activeContext.md`. **Next: TRD §1 Architecture Overview.**
- ✅ **TRD Decision 3 (API §4) + Decision 4 (Auth/Session §5) (2026-09-07).** **API = tRPC** (data ops) + HTTP upload; **3 API Lambdas** (app-api/import-api/admin-api) + SQS import-worker; procedure catalog mapped to AP-1…AP-34 (current price embedded in eval/chart; `resolveSymbol`/price-cache/login internal); conventions = cursor pagination, typed errors, Zod, **poll `import.getStatus`**. **Auth = HYBRID session** (JWT access token + refresh token) stored in a **dedicated DynamoDB sessions table** (⏳ **3rd table — ADR-003 addition to flag in data model**); refresh token in `HttpOnly`+`Secure`+`SameSite=Lax` path-scoped cookie, access token in-memory Bearer; libs `openid-client`+`jose` (backend-owned; Passport/Auth.js rejected); enforcement via tRPC middleware + public/protected/admin procedure types (verified `userId` from token, `USER#` key rooting = structural isolation). Full record in `activeContext.md`. **Next: TRD Decision 5 (§1 Architecture Overview, or §6 External Integrations / OQ-E).**
- ✅ **TRD Decision 2 — Technology Stack (2026-09-07).** **Backend language = TypeScript** (Node/Lambda; unifies with the TS frontend; best single-table DynamoDB tooling; Python held as a deferred escape-hatch for XIRR/heavy-parsing only if TS proves inadequate). **Data-access = ElectroDB** for the complex main table + **raw SDK v3** for the trivial price-cache table. **Frontend = a single Next.js app** for both public (SEO/SSR-SSG) + private pages (chosen over React+Vite SPA and over a two-app split — public pages planned from start, one framework avoids UI duplication + un-retrofittable SEO). Verified data model is **exactly 2 tables** (FR-H5 `PRICEHIST#` lives in the existing aux price-cache table, not a 3rd table — §9.6). Full record in `activeContext.md`. **Next: TRD Decision 3 (API design & contracts, §4).**
- ✅ **TRD Decision 1 — Runtime + Import Pipeline (2026-09-07).** Runtime = **serverless** (Lambda + API Gateway). Import execution = **async via SQS + DLQ** (rejected synchronous, fire-and-forget async invoke, and DynamoDB-Streams-as-trigger — the last after showing Streams still needs an `OnFailure` queue, re-fires on the `COMPLETE` write, and has unverified per-shard poison-blocking). Reliability rests on **idempotent dedup (ADR-005)**; queue just supplies safe retries; DLQ + bounded retries handle poison jobs. Import pipeline reshaped to a **richer two-gate flow** (Upload → Layer-1 + detect → **Gate 1: Proceed to Import** → async normalize + **park normalized batch in temp S3** → notify → preview shows normalized rows + skipped/unsupported → **Gate 2: Confirm** → async write + promote raw file to permanent S3). `ImportedFile` gains a **status spine** (`UPLOADED→PROCESSING→PREVIEW_READY→COMMITTING→COMPLETE` +`FAILED`/`CANCELLED`) and is **created-once-at-upload, updated-through-lifecycle**. TRD principle: **invocation-agnostic import pipeline**. Full record + **pending source-doc reconciliation checklist** (L4/L5/D13/ADR-016/§10.8 write-timing) in `activeContext.md`. **Next: author this into `TRD.md`, then TRD Decision 2 (backend language/runtime).**

### Documentation

- ⚠️ `MASTER_REQUIREMENTS.md` — locked decisions D-001 through D-039 + open questions OQ-006, OQ-007 — **superseded** (relational-era; now in `unimportant/`, reference-only)
- ⚠️ `DATA_MODEL.md` — physical schema (DDL, sample data, indexes) — **superseded** (PostgreSQL; now in `unimportant/`, reference-only)
- ⚠️ `DATA_MODEL_ANALYSIS.md` — full formal analysis (3,125 lines, 23 sections) — **superseded** (PostgreSQL; now in `unimportant/`, reference-only)
- ✅ `DYNAMODB_DATA_MODEL.md` — authoritative DynamoDB data model (13 sections; current SoT)
- ✅ `memory-bank/` — persistent context for AI sessions (this directory)

### Schema Design

> ⚠️ **The four bullets below are relational-era (PostgreSQL) and are superseded by the DynamoDB model** (`DYNAMODB_DATA_MODEL.md`); the underlying _requirements_ they encode carry over, but their relational _implementations_ do not. Retained for history only.

- ⚠️ _(superseded, relational-era)_ 11 tables, 88 columns, 13 FK relationships, 9 composite unique constraints designed and reviewed
- ⚠️ _(superseded, relational-era)_ Two-layer deduplication strategy (file-hash + broker_trade_id) defined — _requirement carries over: **Layer 1** still hashes the file bytes (the fingerprint is now the `FILE#<contentHash>` key instead of a column); **Layer 2** replaces the `broker_trade_id` UNIQUE constraint with deterministic `tradeId`/`cashflowId` + `attribute_not_exists` (ADR-005)_
- ⚠️ _(superseded, relational-era)_ Import pipeline (8 steps) documented with worked examples for all 5 file types
- ⚠️ _(superseded, relational-era)_ Index strategy (10 indexes) defined and justified — _DynamoDB model uses 3 GSIs, §8_
- ⚠️ Database engine selection — **PIVOTED: PostgreSQL → DynamoDB (2026-06-17).** The
  PostgreSQL 15+ lock (DMA §23) is **superseded.** DynamoDB is now the real target database.
  See `activeContext.md`.

---

## What's Left (Outstanding Work)

### Import-Mechanism Source-Doc Edit (✅ COMPLETE — all 9 batches + final pass; see `HANDOFF-DOCS-EDIT.md` §3.5 tracker)

> Writing the locked import decisions D1–D14 into the source docs, in 9 batches. **Resume safety net** — batch-by-batch edit log (exact sections/files touched):

- **Batch 1 (2026-09-06)** — ✅ `DYNAMODB_DATA_MODEL.md` §5: added **ADR-015** (admin-managed BrokerMapper + normalization step; `columnMapping` four kinds `direct`/`lookup`/`classify`/`extract`) and **ADR-016** (raw uploaded-file storage in S3; `s3Key` = relative `<userId>/<contentHash>`, forward-ref to §7.3 for Batch 5). Also extended the §5 intro range “ADR-011 through ADR-014” → “ADR-011 through ADR-016.” No other sections touched.
- **Batch 2 (2026-09-06)** — ✅ `DYNAMODB_DATA_MODEL.md`: §3.1 added **BrokerMapper** as entity row 12 (global/admin-owned) + a "why global/admin" callout; §3.2 disambiguated the "Broker" non-entity bullet vs BrokerMapper; §3.3 added the `ImportedFile → BrokerMapper` relationship row; §3.4 added BrokerMapper to the conceptual-map diagram + caption; §4.2 added **AP-33** (match-mapper-on-upload) and **AP-34** (admin create/update/retire mapper) + a "neither needs a new GSI" note. **Deferred (not Batch 2 scope):** the prose range-labels "AP-1 … AP-32" at §5/§7/§12 and the §12.1 mapping-table rows for AP-33/AP-34 are left for Batch 5 (they need the BrokerMapper item **key**, defined in §7) / the final consistency pass. **Addendum (post-review fixes):** added the §3.1 **Account-identity callout** (HANDOFF Batch 2 bullet 2) — deterministic per-(user,broker) `accountId` (`acc_rh`/`acc_fid`/`acc_zer`), `broker` separate attr, find-or-create by `(userId,broker)`, `accountType` optional (Zerodha `Individual`; Fidelity/RH unset), multi-account-per-broker = Phase 2 (keeps "Account" naming; rename is Batch 4). Realigned the §3.4 diagram BrokerMapper box (cosmetic). **To close in Batch 5:** (a) AP-34 verb — doc says "retire"; spec/`activeContext.md` L1 say "delete" — pick ONE verb across AP-34/ADR-015/§12.1; (b) bump "AP-1 … AP-32" → "AP-1 … AP-34" at §5/§7/§12 + add §12.1 mapping rows for AP-33/AP-34.
- **Batch 3 (2026-09-06)** — ✅ `DYNAMODB_DATA_MODEL.md` **isolated GSI "by account"→"by broker" sweep** (all-or-nothing, landed as one unit): GSI1 key `USER#<userId>#ACCT#<accountId>` → `USER#<userId>#BROKER#<broker>` at §7.4 (Trade `GSI1PK` row), §8.1 (heading "Trades by Broker", key lines, rationale callout), §10.3 (retitled "Per-Broker & Multi-Currency Isolation"), §12.1 AP-11 row; AP-11 wording "filtered to one account"→"one broker" at §4/§8.1/§12.1/§12.2; §3.1 row-2 + §3.3 relationship reworded per-broker. **§11 example collapse (Option 1):** Alex's three Fidelity accounts (`acc_fid_ind`/`_roth`/`_401k`) merged into one `acc_fid` (accountType unset, displayName "Fidelity") — one consolidated account item (removed 2 extra), all Trade/Cashflow `accountId` re-pointed, partition view collapsed to one row, §11.1 cast + §11.3 intro + two §11.5 trade headings reworded; added a Phase-1-consolidation note. **Verified:** no duplicate `tradeId`/`cashflowId` from the merge (ids don't embed accountId); grep clean — no `#ACCT#`, no `acc_fid_*`, no stray "by account" GSI language (the one remaining "by account" at §8.1 is the intentional "coincide" note); §3.6 GSI1PK guard holds (§7.4==§8.1==§10.3==§12.1). Entity name still "Account" (rename = Batch 4). **Post-review residual fixes (2 misses caught in review):** (a) §6.5 GSI forward-map preview "GSI1 — filtering trades to a single account (AP-11)" → "single **broker**" (+ AP-10 "across all accounts" → "all brokers") — this line was outside my original `filtered to one account` grep anchor; (b) §8.1 `GSI1SK` caption "that account's trades" → "that broker's trades". Re-grep now fully clean: no `single account` / `that account's trades` / `across all accounts` / `#ACCT#` / `trades-by-account` remain (only the intentional §8.1 "coincide" note references "by account").
- **Batch 4 (2026-09-06)** — ✅ `DYNAMODB_DATA_MODEL.md` **isolated `Account`→`BrokerAccount` rename** (all-or-nothing). **SK-token decision: keep `ACCOUNT#<accountId>`** + `accountId`/`accountType` field names unchanged (renaming = needless churn / implied key migration); only the conceptual entity name + `entityType` value change. **Renamed (bucket A):** §3.1 entity row 2 + row-2 callout, §3.2 broker bullet, §3.3 both relationship rows, §3.4 diagram box, §6 "what lives here" + §6.3 overloading enumeration + §6.4 entity-location table, §7.2 heading + `entityType` "Account"→"BrokerAccount", the 4 Trade/Cashflow denorm cells ("from Account)"→"from BrokerAccount)"), §7.11 key-map table, §10.3 prose, §11.3 heading + all 3 example-item `entityType` values, §11.13 partition-view `entityType` column (3 rows). **Kept (bucket B/C, deliberate):** `accountId`(34×)/`accountType`/`ACCOUNT#`(15×)/`acc_*`; auth "user account", §13.1.6 "OQ-G Account Linking", "account holder", lowercase generic "account(s)", §4.1 "Users & Accounts" heading. **Verified (widened whole-word greps):** 0× `"entityType":"Account"` remaining, 3× `"BrokerAccount"`; no entity-sense capital-A "Account" left except the 3 intentional keeps; re-padded 4 table cells for source alignment (Priya partition row left as-is — 12-wide col vs 13-char value, renders fine). **Deferred to Batch 5 (expected transient):** §7.2 `accountType` example values (Roth/401k) + "several per broker" in-plain-terms line get their D9 refinement then.
- **Batch 5 (2026-09-06)** — ✅ `DYNAMODB_DATA_MODEL.md` item definitions (§7) + BrokerMapper. **§7.2 BrokerAccount:** `accountType` now **optional** (Zerodha `Individual`; Fidelity/RH unset — never guessed, D9), `accountId` = deterministic per-(user,broker) id, in-plain-terms line → one-account-per-broker (closes the Batch-3/4 deferral). **§7.3 ImportedFile:** added **`s3Key`** (relative `<userId>/<contentHash>` only, bucket in app config — matches ADR-016 per §3.6 guard) + a Why note (S3-first write order). **§7.4 Trade:** added `rawAction`, `eventType`, `securityId`+`securityIdType` (D8), `occurrence` (RH dedup), `brokerTradeId` (Zerodha dedup); updated `tradeId` fingerprint line (per-broker distinguisher, D2); `instrumentType` note (all opts→Option, IndexOption→P2/OQ-T); `optionDetails` note (D10 per-broker parse). **§7.5 Cashflow:** `cashflowType` **6→10 (D4 final: BUY,SELL,DEPOSIT,WITHDRAWAL,DIVIDEND,INTEREST,INCOME,FEE,ROYALTY,ADJUSTMENT — distinct INTEREST per D4)**; added `includeInXIRR` (bool), `cashBalance` (Fidelity dedup), `rawAction`; cashflowId fingerprint note. **New §7.13 BrokerMapper item** (GLOBAL#BROKERMAPPER / MAPPER#<fileType>): `fileType`,`broker`,`headerFingerprint`,`columnMapping` (four kinds direct/lookup/classify/extract — matches ADR-015 per §3.6 guard),`version`,`updatedAt` + Why note. **§6.5** forward-map + **§7.11** summary: added BrokerMapper row. **Closed deferred to-dos:** §12.1 added AP-33/AP-34 mapping rows (+ §12.2 read-back note); "AP-1 … AP-32" → "AP-1 … AP-34" (4 spots: §5/§7/§12 intro/§12.1 heading); **AP-34 verb "retire"→"delete"** at §4.2 (Batch-2 inconsistency) + consistent "delete" in §7.13/ADR-015/§12.1 (Q2); fixed stale §11.6 "all six cashflowType values" → "a representative span of the 10". **Verified:** §3.6 guards hold (s3Key relative-only, columnMapping 4 kinds, GSI1PK); no stray "retire" verb; enum consistent. **Note for Batch 9:** the stale D1 9-category list in activeContext should get a "see D4 for final enum" pointer.
- **Batch 6 (2026-09-06)** — ✅ `DYNAMODB_DATA_MODEL.md` §10 Import & Normalization pipeline. **§10.1 revised (in place):** Layer-2 now documents the **per-broker distinguisher** (D2) — Zerodha `brokerTradeId` / Fidelity `cashBalance` / Robinhood `occurrence #N` — with the "why the natural key alone isn't unique for RH/Fidelity" rationale; the known-limitation callout narrowed to the D2-downgraded **Robinhood-only OQ-A** (distinct identical rows never in one file). **New §10.8 "Import Pipeline: Detection, Dedup & Raw-File Capture":** L4 order (Layer-1 hash → header-fingerprint detect/AP-33 + hard-stop L3 with D9-Q5 specific-vs-generic msg → accountId find-or-create by (user,broker) → minimal parse + Layer-2 counts → normalize NEW rows only → preview L5 → confirm D13 → S3-first write D14/ADR-016 → ImportedFile+`s3Key` → Trades/Cashflows w/ attribute_not_exists); forward-only correction L9/OQ-M. **New §10.9 "Normalization: Per-Broker Row Interpretation":** four `columnMapping` kinds (ADR-015); transaction catalogue D1→D4 enum + Fidelity `classify` word-set; Fidelity verbs D9 (REINVESTMENT/DRIP→BUY, Contributions→BUY/MutualFund, Transfers→inert, TRANSFERRED→excluded, EFT→DEPOSIT/WITHDRAWAL D5, SPAXX/FDRXX hidden); events D3; fees L7; option parsers D10 (all→Option, FUT+non-match safe-fail, IndexOption→OQ-T); dates D11; number/RFC-4180 + footer skip D12; `rawAction` preserved L8; safe-fail throughout. **Scope guards honored:** no §13 OQ rewrites (Batch 7 — only forward-pointers), no §11 worked example (Batch 7), no PRD/FEATURES (Batch 8). Section Index unchanged (lists top-level sections only). **Verified:** per-broker fingerprints match §7.4/§7.5; refs to ADR-015/016/AP-33/§7.13 correct; dual-audience style; clean §10.9→§11 boundary. **Note for Batch 9 (2nd item):** reconcile the SPAXX `SWEEP` inconsistency in `activeContext.md` — the D4 classify table (~line 135) emits `cashflowType=SWEEP`/`*(stored, sweep)*`, but the locked 10-value enum has **no** `SWEEP` value (source doc correctly assigns no cashflowType to a hidden sweep). Fix the memory bank so it stops implying a non-existent `SWEEP` type.
- **Batch 7 (2026-09-06)** — ✅ `DYNAMODB_DATA_MODEL.md` §11 worked example + §13 open questions. **New §11.15 "Worked Example — One Raw Row Through the Pipeline":** a Fidelity option-sale row (`6.30×100−0.65−0.04=629.31`, L7) traced raw CSV → column-mapping table (four kinds) → normalized **Trade + Cashflow** JSON (post-Batch-3/4 state: `acc_fid`, `GSI1PK=USER#u_alex#BROKER#Fidelity`, new attrs `rawAction`/`optionDetails`/`cashBalance`/`includeInXIRR`); exercises classify(D7)+option-extract(D10)+date(D11)+net-Amount fees(L7)+Trade&Cashflow(ADR-011); includes the D2 dedup-fingerprint line. **§13 rewrites:** OQ-A reframed to the **D2-narrowed** Robinhood-only residual (matches §10.1 callout); **OQ-J → RESOLVED (L7)** net-Amount confirmed / commission+fees display-only, with a non-blocking per-column residual note; OQ-C disambiguated — explicit `REINVESTMENT`→BUY is settled (§10.9) vs the open companion-BUY auto-reinvest question (audit follow-up #1). **Added OQ-K/L/M/N/O/P/Q/R/S/T** as structured blocks (K largely-resolved D5, M partially-resolved D14, others verification/Phase-2). Updated §13.1 intro ("three"→full set), §13 note ADR range →016, and the closing callout's resolved-list. **Verified:** all OQ IDs A–T present, numbered 13.1.1–13.1.20, no gaps/dupes; §11.15 renders. **Scope:** no PRD/FEATURES (Batch 8), no memory-bank (Batch 9). **Batch-9 queue (2 items):** (1) D1 stale 9-category list → "see D4"; (2) SPAXX `SWEEP` non-enum-value reconciliation.
- **Batch 8 (2026-09-06)** — ✅ `PRD.md` + `FEATURES.md`. **PRD:** FR-I2 → single-account Fidelity in Phase 1 (multi-account-within-broker = Phase 2); FR-I4 → D13 wording ("no manual entry of trade data; one-time confirmation on import"); import acceptance criteria — replaced the "three distinct accounts" line with single-account-consolidation + folded in the **D4 canonical import statement** (what is stored: dup/SKIP excluded, buy/sell→Trade+Cashflow, cash-only rows→Cashflow, events→Trade, includeInXIRR flag, stored≠shown); FR-H2 "single account"→"single broker"; FR-M1 → multiple **brokers** (multi-account-per-broker deferred to P2); changelog bump **1.2→1.3** + Last-updated 2026-09-06 (changelog phrased as "per-broker / broker-account language alignment" — PRD prose keeps plain-language "broker account", NOT the code-entity `BrokerAccount`, which is correct for a product doc). **FEATURES:** §5.1 → D13 wording (never type trades by hand; one-time confirmation); §5.10 → per-broker in Phase 1, multi-account-per-broker = Phase 2. **Also:** added `contractMultiplier`/`lotSize` note to §7.4 `optionDetails` in the data model (resolves Batch-7 audit §7↔§11 note #1). **Kept intentionally:** FR-A6/OQ-G "account linking" (auth sense); §7.11/§5.10/F10 "Multiple Accounts & Currencies" section titles (still cover multi-currency + Phase-2 multi-account); §6 "multi-account/currency" capability label. **Verified:** no stray "no manual data entry" / "three accounts" / "multiple accounts with the same broker" as Phase-1 claims. **Scope:** memory-bank untouched (Batch 9).
- **Batch 9 (2026-09-06)** — ✅ Memory-bank reconciliation. **systemPatterns.md:** Account→BrokerAccount; GSI1 "by broker" (`USER#<userId>#BROKER#<broker>`); added BrokerMapper item + `GLOBAL#BROKERMAPPER` exception; ADR list **14→16** (+ADR-015/016 rows); dedup mechanism notes per-broker distinguishers; new Import&Mapper AP row; soft-spots table — OQ-A narrowed, OQ-J resolved, OQ-K–OQ-T added. **techContext.md:** GSI1 label→by-broker; added BrokerMapper + S3 mechanism rows + S3 external-dependency; broker-files fallback→admin BrokerMapper/hard-stop; **flipped "Raw uploaded-file storage" from NOT-decided → decided (S3/ADR-016)**; OQ table OQ-A/J + K–T. **projectbrief.md:** Fidelity row→one-account-Phase-1/multi=P2; import-mechanism "open design topic"→**decided** (BrokerMapper/ADR-015 + S3/ADR-016, D1–D14 locked). **productContext.md:** onboarding flow→detect+find-or-create+one-time-confirm (D13); Broker Account glossary→one-per-broker Phase 1. **Two queued fixes closed in activeContext.md:** (1) D1 9-category list now points to **D4 for the final 10-value enum** (distinct INTEREST); (2) SPAXX `SWEEP` reconciled — flagged as **NOT** a distinct `cashflowType` (stored+hidden `includeInXIRR=false`), in both the D1 category table and the D4 classify table. **Status flipped:** activeContext Current Focus + progress line-30/line-168 headers → "WRITTEN INTO SOURCE DOCS / COMPLETE". Kept `ACCOUNT#` SK-token examples (physical prefix retained by decision). Next: final full-document consistency pass.
- **Final consistency pass (2026-09-06)** — ✅ full-document sweep across `DYNAMODB_DATA_MODEL.md` + `PRD.md` + `FEATURES.md` + memory bank. **Verified clean:** no stray entity-sense "Account" / `entityType:"Account"` / `#ACCT#` GSI keys (only the intentional §8.1 "by broker/by account coincide" note); GSI1 uniformly `USER#<userId>#BROKER#<broker>` (§7.4==§8.1==§10.3==§12.1); ADR numbering → 016 everywhere (data model §5 intro/§13 note; systemPatterns "16 ADRs"); `cashflowType` = 10-value D4 enum with no lingering "six"/`SWEEP`; s3Key relative-only (§7.3==ADR-016); columnMapping four kinds (§7.13==ADR-015==§10.9); 20 OQ blocks (13.1.1–13.1.20, no gaps/dupes); PRD/FEATURES/data-model/memory-bank mutually agree on D13 confirmation-on-upload + single-account-per-broker Phase-1. **ALL 9 batches + final pass complete — the import-mechanism source-doc edit phase is DONE.**

### DynamoDB-Native Data Model — FEATURES.md-Derived (✅ COMPLETE 2026-06-26)

**Now COMPLETE.** Designing the project's database from scratch as a DynamoDB-native model,
derived **purely from `FEATURES.md`**, as a **replacement** for the PostgreSQL design. All
**13 sections** of `DYNAMODB_DATA_MODEL.md` are written across **8 reviewed batches**, with
the final consistency pass applied (Session 9). Full details in `activeContext.md`.

**Locked context:**

- Output: single new `DYNAMODB_DATA_MODEL.md` at repo root (append-only; no per-batch files)
- Source of truth: `FEATURES.md` only (relational docs deliberately excluded)
- Architecture: pragmatic mix — main single-table + separate TTL-driven price-cache table
- Philosophy: access-pattern-first; per-user isolation rooted in every key
- Includes a dedicated **ADR / rationale** section + inline "why" callouts (do's/don'ts reference)
- Workflow: 8 batches, write 2 sections at a time → stop → review gate → proceed/revise

**Batch progress (13 sections across 8 batches):**

| Batch | Sections                                                 | Status             |
| ----- | -------------------------------------------------------- | ------------------ |
| 1     | §1 Introduction & Scope · §2 Design Principles           | ✅ Done (reviewed) |
| 2     | §3 Entities & Relationships · §4 Access Patterns Catalog | ✅ Done (reviewed) |
| 3     | §5 Design Decisions & Rationale (ADRs)                   | ✅ Done (reviewed) |

| 4 | §6 Table Design Overview | ✅ Done (reviewed) |
| 5 | §7 Main Table — Item Definitions · §8 GSIs | ✅ Done (reviewed) |
| 6 | §9 Auxiliary Table — Price Cache · §10 Handling the Tricky Requirements | ✅ Done (reviewed) |
| 7 | §11 Example Items · §12 Access Pattern → Implementation Mapping | ✅ Done (reviewed) |
| 8 | §13 Open Questions & Assumptions + consistency pass | ✅ Done — **DOCUMENT COMPLETE** |

**Memory-bank rewrites (✅ DONE 2026-07-10):** `systemPatterns.md` (DynamoDB single-table
design) and `techContext.md` (DynamoDB tech stack) have been rewritten from
`DYNAMODB_DATA_MODEL.md`, replacing the retired PostgreSQL content; `README.md` banners
updated. The memory bank is now fully consistent with the DynamoDB model.

### Implementation Work (Not Started)

- [ ] Write the TRD (settles OQ-F session strategy + OQ-E historical-price provider/range)
- [ ] Resolve OQ-B — Zerodha F&O OPEN/CLOSE position tracker design
- [ ] Resolve OQ-C — verify DRIP companion BUY/DIVIDEND behavior with real Fidelity/Robinhood files. **Evidence gathered (D9):** `fidelity.csv` shows the pairing on same day / same security — `DIVIDEND RECEIVED HRB +4.20` + `REINVESTMENT HRB −4.20` @ 0.131 sh (also HPQ +9/−9/0.473; IDT +0.70/−0.70/0.015). Phase-1 rule locked: DIVIDEND + a separate `REINVESTMENT→BUY` ("DRIP") row, cash nets to zero, no double-count. Robinhood DRIP pattern still to verify.
- [ ] Choose backend language/framework
- [ ] Provision the two DynamoDB tables + 3 GSIs (main `BeyondFolio` + price-cache) and TTL config
- [ ] Implement self-managed OAuth 2.0 flow (Google) + AuthIdentity lookup + first-login provisioning (ADR-013)
- [ ] Implement import handling per broker file type — 4 types (`ROBINHOOD_ACTIVITIES`, `FIDELITY_SINGLE_ACC_ACTIVITY`, `ZERODHA_EQUITY`, `ZERODHA_FO`); import mechanism is decided (admin-managed BrokerMapper, ADR-015)
- [ ] Implement import pipeline (parse → resolve symbols → two-layer dedup → write Trade+Cashflow)
- [ ] Implement XIRR calculation engine (per-currency timeline + derived-holdings valuation)
- [ ] Implement journal Evaluate flow + atomic TagScorecard counter updates (ADR-006)
- [ ] Implement price cache refresh from external market-data services (US + India)
- [ ] Implement FR-H5 trade price chart (markers via GSI3 + `PRICEHIST#` historical-series cache, ADR-014)
- [ ] Build frontend (framework TBD)
- [ ] Choose hosting; deploy

---

## Locked Decisions (D-001 → D-039)

Cross-reference to `MASTER_REQUIREMENTS.md` and `DATA_MODEL_ANALYSIS.md` §19. Summarized here for quick reference.

| ID    | Topic    | Summary                                                                 |
| ----- | -------- | ----------------------------------------------------------------------- |
| D-001 | Schema   | `transaction_category` is unified — no broker-specific categories       |
| D-002 | Schema   | `charge_type` only set when `transaction_category = BROKER_FEE`         |
| D-003 | Schema   | Zerodha F&O ISIN always blank — schema allows NULL                      |
| D-004 | Pipeline | One row in `import_jobs` per file upload                                |
| D-005 | Dedup    | Layer 1 — SHA-256 of raw file bytes                                     |
| D-006 | Dedup    | Layer 2 — `(user_id, broker_trade_id)` unique constraint                |
| D-007 | Security | `user_id` injected backend-side, never from file                        |
| D-008 | Schema   | `transactions.amount` is direct signed column, not derived              |
| D-009 | Schema   | `settlement_date` nullable — Zerodha doesn't provide                    |
| D-010 | Security | `account_number` plaintext (Phase 2 encryption)                         |
| D-011 | Schema   | `transactions.broker_account_id` always NOT NULL                        |
| D-012 | Schema   | `instruments` stores only underlying securities — no options            |
| D-013 | Journal  | `result` is ENUM nullable — system-written only                         |
| D-014 | Cache    | `price_cache` unique key `(symbol, exchange)` — UPDATE in place         |
| D-015 | Schema   | `currency` is direct column on `transactions`                           |
| D-016 | Schema   | All PKs are UUID, except junction tables                                |
| D-017 | Schema   | `options_details` is separate 1:1 table, not inline columns             |
| D-018 | Schema   | Zerodha `client_id` stored as `account_number`                          |
| D-019 | Schema   | `transactions.amount` is NOT NULL                                       |
| D-020 | Schema   | `journal_entry_tags` uses composite PK — no surrogate UUID              |
| D-021 | Options  | `option_action` NULL for EXPIRATION/ASSIGNMENT                          |
| D-022 | Schema   | `instruments.type` enum — no OPTION value                               |
| D-023 | Schema   | `broker_accounts` UQ `(user_id, broker, account_number)`                |
| D-024 | Schema   | `corporate_action_symbol_map` UQ `(old_symbol, effective_date, broker)` |
| D-025 | Schema   | `instruments.exchange` nullable; app-layer upsert handles dedup         |
| D-026 | Schema   | `instruments.name` NOT NULL                                             |
| D-027 | Schema   | `journal_entries.updated_at` maintained by app or trigger               |
| D-028 | Cache    | `price_cache` TTL is app config, not schema concern                     |
| D-029 | Schema   | `transactions.symbol` redundant alongside `instrument_id` — intentional |
| D-030 | Pipeline | Hard deletes only at import-job level via CASCADE                       |
| D-031 | Pipeline | `import_jobs.broker_account_id` NULL for Fidelity                       |
| D-032 | Dedup    | Robinhood ACH same-day same-amount collision accepted                   |
| D-033 | Schema   | `import_jobs.file_type` ENUM (5 values)                                 |
| D-034 | Cache    | `price_cache.exchange` includes BSE                                     |
| D-035 | Pipeline | `corporate_action_symbol_map` auto-populated from REORG rows            |
| D-036 | Schema   | `transactions.exchange` NULL for Fidelity/Robinhood                     |
| D-037 | Journal  | `journal_entries` is 1:N per transaction (multiple sources)             |
| D-038 | Journal  | `result` computed on Evaluate button click                              |
| D-039 | Journal  | `tags.wins` / `tags.losses` counters incremented atomically             |

---

## Open Questions (Pending Resolution)

| ID         | Topic                                                                                                                                                                                                                 | Severity | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **OQ-006** | Zerodha F&O OPEN vs CLOSE detection from `trade_type` alone                                                                                                                                                           | **High** | ⏳ TBD — proposed: in-memory position tracker by execution time order                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **OQ-007** | Fidelity/Robinhood DRIP companion DIVIDEND row behavior                                                                                                                                                               | Medium   | ⏳ TBD — needs verification with real broker files                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **OQ-D**   | User profile provisioning mechanism                                                                                                                                                                                   | Low      | ✅ **Resolved** — first-login provisioning (mint `userId` + write User & AuthIdentity idempotently on first Google sign-in; ADR-013)                                                                                                                                                                                                                                                                                                                                                                       |
| **OQ-F**   | Session/token strategy after sign-in (app-signed JWT vs. server session; refresh/logout/revocation)                                                                                                                   | Medium   | ⏳ Open — deferred to the TRD (ADR-013)                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **OQ-E**   | Historical price series sourcing for the FR-H5 trade chart — which provider + exact range/granularity                                                                                                                 | Low      | ✅ **Resolved (2026-09-22)** — providers chosen: INR → Zerodha Kite, USD → Twelve Data, routed by currency; cache keyed `(canonicalSymbol, currency)`; pattern lazy fetch + 1-day reuse (ADR-014). NSE/BSE same-INR collapse accepted (data model OQ-E2)                                                                                                                                                                                                                                                   |
| **OQ-G**   | Phase-2 account-linking (Google↔Zerodha) — link a second AuthIdentity to the same `userId`                                                                                                                            | Low      | ⏳ Open — Phase 2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **OQ-H**   | Admin user management (disable/delete a user) — stated but not modeled                                                                                                                                                | Medium   | ⏸️ **On hold** — out of Phase 1; would need a users-enumeration index + a 2nd exception to ADR-002 + a `status` flag on `User`                                                                                                                                                                                                                                                                                                                                                                             |
| **OQ-I**   | Zerodha fees/charges omitted from INR XIRR in Phase 1 (charges only in the non-ingested P&L report; tradebooks carry none)                                                                                            | Medium   | ⏳ Open — accepted for Phase 1 (INR XIRR shown fee-exclusive with a notice); capturing Zerodha charges deferred to Phase 2. USD (Fidelity/Robinhood) fees **are** captured                                                                                                                                                                                                                                                                                                                                 |
| **OQ-J**   | Fidelity commission/fees columns imported as `FEE` cashflows (part of USD XIRR) — exact semantics unconfirmed                                                                                                         | Low      | ✅ **Resolved (D2/L7)** — Fidelity trade `Amount` is already net of commission+fees → use net Amount as the cash leg; keep commission/fees display-only (no separate FEE row)                                                                                                                                                                                                                                                                                                                              |
| **OQ-K**   | Fidelity external cashflows + SPAXX sweeps                                                                                                                                                                            | High     | ✅ **Resolved (D5, revised)** — richer `fidelity-2025.csv` shows Fidelity **DOES** emit external cashflows: `Electronic Funds Transfer Received`→DEPOSIT, `Paid`→WITHDRAWAL (`includeInXIRR=true`, Cashflow-only). Fidelity XIRR now properly funded from these rows; SPAXX sweeps stay `includeInXIRR=false`/hidden; "not included" notice narrowed to degraded-mode (uploads lacking transfer rows); `Cash Balance` reconciliation demoted to a Phase-2 fallback. Residual verification folded into OQ-Q |
| **OQ-L**   | Stock-vs-ETF classification for the FR-H5 chart                                                                                                                                                                       | Medium   | ✅ **Resolved (D6)** — Phase-1: default `instrumentType=Stock`, upgrade to `ETF` only on an unambiguous "ETF" Description marker (no FUND/TRUST/SHARES → no false positives); never a wrong ETF, may under-label some ETFs (harmless — chart renders both). Phase-2: authoritative symbol lookup (likely OQ-E provider) backfills true types                                                                                                                                                               |
| **OQ-M**   | Mapper correction → retroactive re-import of already-imported rows                                                                                                                                                    | Medium   | 🟡 **Partially resolved (D14)** — the raw-file-storage _prerequisite_ is now met in Phase 1 (every confirmed import's original file kept in S3 at `<userId>/<contentHash>`, SSE-S3, keep-forever, referenced by `ImportedFile.s3Key`); the retroactive-reprocess + "undo an import" _engine_ remains Phase 2                                                                                                                                                                                               |
| **OQ-N**   | RSU vesting mechanics (JOURNALED-RSU tax rows excluded from XIRR; zero-cost RSU share-ins as BUY@0; cost-basis nuance for Phase-2 P/L)                                                                                | Low      | ⏳ Open — verify against a real vesting event                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **OQ-O**   | Corporate-action _interpretation_ (split ratio, merger symbol-swap) needs data the files don't fully give — `SPL` carries top-up qty (e.g. `0.4434`) not the ratio; `MRGS` gives in/out quantities with an `S`-suffix | Medium   | ⏳ Open — Phase-1 stores EVENT rows inert with `eventType` (D3); interpretation deferred to the Phase-2 execution-ordered position tracker (ties to OQ-B); verify against real corporate-action examples                                                                                                                                                                                                                                                                                                   |
| **OQ-P**   | Income-tail category + XIRR-membership mappings (D4): `SLIP`/`GDBP` → INCOME; `LCAP`/`SCAP`/`MDIV` → DIVIDEND; `GDBP` counted in XIRR as a promo bonus                                                                | Low      | ⏳ Open — verify against real broker files/edge cases; `includeInXIRR` is flippable per row without re-modeling if an assumption proves wrong                                                                                                                                                                                                                                                                                                                                                              |
| **OQ-Q**   | Fidelity `Action` word-set classifier + `Symbol`-column reliability (D7)                                                                                                                                              | Low      | ⏳ Open — rules validated on the 319-row dummy file (0 ties, 0 unmapped); verify against a real multi-year export (rare verbs: average-price/agency-cross/transfers/fee-reversals; confirm `Symbol` populated on all security rows). Safe-fail to `unmapped` protects XIRR meanwhile                                                                                                                                                                                                                       |
| **OQ-R**   | Security identifier capture (D8): RH CUSIP format in `Description`; Zerodha ISIN reliability (EQ populated / FO blank)                                                                                                | Low      | ⏳ Open — verify against real exports; Fidelity has NO CUSIP/ISIN (cross-broker id-linking inherently one-sided); `securityId` is optional/non-identity/Phase-2-facing                                                                                                                                                                                                                                                                                                                                     |
| **OQ-S**   | Date parsing assumptions (D11): `YY→20YY`; RH/Fidelity have no intraday time                                                                                                                                          | Low      | ⏳ Open — verify against a longer real export; malformed dates safe-fail to a flagged row meanwhile                                                                                                                                                                                                                                                                                                                                                                                                        |
| **OQ-T**   | Zerodha IndexOption vs Option classification (D10) — all Zerodha options tagged `Option` in Phase 1                                                                                                                   | Low      | ⏳ Open — Phase-2 backfill via a known-index-set / instrument lookup (NIFTY/BANKNIFTY/FINNIFTY/SENSEX/MIDCPNIFTY…); mirrors OQ-L (Stock→ETF)                                                                                                                                                                                                                                                                                                                                                               |

> **✅ DONE — Import Mechanism (BrokerMapper + normalization pipeline) is locked AND written into the source docs** (9-batch source-doc edit complete; see §3.5 tracker + Batch-1–89 notes above, and the final consistency pass). The detailed decision history below is retained for reference; the authoritative record is **`activeContext.md` → "Current Focus (2026-09-06) — … WRITTEN INTO SOURCE DOCS (COMPLETE)"**: 9 locked decisions **L1–L9**; **D1 Transaction-Type Catalogue ✅ LOCKED** (per-broker code→category map; new categories INCOME/ROYALTY/ADJUSTMENT; `includeInXIRR` flag; `rawAction` preserved); **D2 Layer-2 dedup fingerprint ✅ LOCKED** (Zerodha `trade_id`; Fidelity `Cash Balance`; Robinhood occurrence-index; applies to Trades AND Cashflows); **D3 Non-trade position events ✅ LOCKED** (hybrid — stamp `eventType` enum on Trade now; EVENT rows inert, shown in Trade History as non-cash informational rows; EVENT Trades excluded from XIRR structurally, NOT via `includeInXIRR`; interpretation deferred to the Phase-2 position tracker); **D4 `cashflowType` enum + XIRR membership ✅ LOCKED** (enum grows 6→10: adds INTEREST/INCOME/ROYALTY/ADJUSTMENT; `includeInXIRR` kept as a per-Cashflow-only boolean; RH `INT`→INTEREST, `SLIP`/`GDBP`→INCOME, `MDIV`/`LCAP`/`SCAP`/`CDIV`→DIVIDEND, Fidelity ROYALTY→ROYALTY, RSU-tax→ADJUSTMENT & SPAXX sweeps → `includeInXIRR=false`, all else true). **D5 Fidelity external cashflows + SPAXX sweeps ✅ LOCKED** (Option D: sweeps `includeInXIRR=false` & hidden; Fidelity external deposits absent & NOT fabricated; Fidelity XIRR shown with a notice; `Cash Balance` reconciliation deferred to Phase 2; OQ-K raised to High & resolved for Phase 1). **D6 Stock-vs-ETF classification ✅ LOCKED** (Option 4a modified: default `instrumentType=Stock`, upgrade to `ETF` only on an unambiguous "ETF" marker; never a wrong ETF; Phase-2 lookup backfills; OQ-L resolved). **D7 `columnMapping` structure ✅ LOCKED** (four kinds: `direct` copy + `lookup` clean code columns + `classify` word-set matcher on free-text Action [whole-word, verb-inclusive, glue-free, largest-set-wins, safe-fail to `unmapped`; validated 0 ties/0 unmapped on 319 Fidelity rows] + shared `extract` for the structured Symbol code; underlying/rawSymbol from the `Symbol` column via `direct`, NOT Action; classify is category-only; OQ-Q logged). **D8 Capture security identifier ✅ LOCKED** (Option 2: add optional `securityId`+`securityIdType` (CUSIP/ISIN) on Trade where present — Zerodha `isin` via `direct`, RH CUSIP via `Description` extract; Fidelity has none; non-identity metadata, stored-but-unused in Phase 1, Phase-2-facing for merger-leg disambiguation & cross-broker linking; the CUSIP _change_ is the merger signal, not constancy; OQ-R logged). New open questions **OQ-K / OQ-L / OQ-M / OQ-N / OQ-O / OQ-P** added above; **OQ-J resolved** (L7) and **OQ-A revised/downgraded** by D2 (Robinhood-only narrow residual; Fidelity/Zerodha immune). Source-of-truth docs (`DYNAMODB_DATA_MODEL.md`, `PRD.md`, `FEATURES.md`) intentionally **not yet edited**. **D9 ✅ LOCKED** — Phase 1 accepts **single-account Fidelity only** (multi-account → Phase 2); `accountId` = opaque per-broker id (`acc_rh`/`acc_fid`/`acc_zer`), deterministic per (user,broker), `broker` a separate attribute (NOT concatenated into the id), find-or-create by (user,broker); **`GSI1PK = USER#<userId>#BROKER#<broker>`** (keys on broker); GSI1/AP-11 reframe "by account"→"by broker" (coordinated sweep); **D2 unchanged** (revision cancelled, `Cash Balance` key verified stable); all Fidelity verbs catalogued LIVE (REINVESTMENT/DRIP→BUY, FDRXX, Contributions→BUY via Description, Transfers→inert, TRANSFERRED→excluded); FR-I2 reframe deferred. **D10 ✅ LOCKED** — Fidelity (`Symbol`) + Robinhood (`Description`) + **Zerodha** (`symbol` + `expiry_date` column; strike via expiry-derived date-token strip; all `Option`, IndexOption→Phase-2/OQ-T; FUT + non-match safe-fail) option parsers all locked. **D11 ✅ LOCKED** (per-broker date parse → canonical `YYYY-MM-DD`; RH/Fidelity month-first, Zerodha ISO `order_execution_time`; `YY→20YY`; safe-fail; display-format toggle → Phase-2 backlog; OQ-S). **D12 ✅ LOCKED** (INPUT→STORAGE number/format cleanup: mandate RFC-4180 CSV parser — all rules on parsed values, no data-level quotes exist; RH strip `$`/thousands-comma + `(...)`→negative; Fidelity/Zerodha plain; `S`-suffix stripped, meaning via eventType (D3); RH multi-line Description handled by parser (equity rows only); Zerodha `order_id` apostrophe strip if used; footer/junk rejected by requiring valid date + recognized Action/Trans Code). **D13 ✅ LOCKED** (reworded "no manual data entry" → confirmation-on-upload; FEATURES §5.1 / PRD FR-I4, physical edit deferred to the batch). **D14 ✅ LOCKED** (raw-file storage — Phase 1 keeps every confirmed import's original file in S3 at `<userId>/<contentHash>`, SSE-S3, keep-forever, `ImportedFile.s3Key`, S3-before-DB write order, backend-only; unblocks Phase-2 reprocess/undo; OQ-M partially resolved). **ALL import decisions D1–D14 ✅ LOCKED and written into the source docs (9-batch edit complete + audited). Active phase: the TRD — see `HANDOFF-TRD.md` (the active handoff).** `HANDOFF.md` = historical design record; `HANDOFF-DOCS-EDIT.md` archived to `unimportant/`. See `activeContext.md` for the authoritative decision record.

---

## Superseded Decisions

| Decision                                                          | Original Status | Outcome                                                                                                      | Status            |
| ----------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------ | ----------------- |
| **PostgreSQL for ALL 11 tables** (DMA §23)                        | ✅ Locked       | Project pivoted to **DynamoDB** (2026-06-17). Relational design retired.                                     | ⛔ **Superseded** |
| **Polyglot persistence analysis** (`DB_ALTERNATIVES_ANALYSIS.md`) | 🔄 In Progress  | Moot — going all-in on DynamoDB makes the per-table "keep in PG vs move" question obsolete. Kept as history. | ⛔ **Superseded** |

> **Note on D-001 → D-039:** these are **relational-era** decisions. The _requirements_ they
> encode (e.g., two-layer dedup, per-user isolation, atomic tag counters, multi-currency)
> still apply and must be satisfied by the DynamoDB model — but their relational
> _implementations_ (FKs, composite UNIQUE constraints, ENUMs, CASCADE) do **not** carry over.
> They are now reference-only.

---

## Session History

| Date       | Session                                   | Highlights                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-09-03 | Import D10                                | **D10 — Option `optionDetails` parse rules ✅ FULLY LOCKED (Zerodha added).** Closes the last open import decision → **all D1–D14 locked.** Zerodha parser (runs only on `segment=FO`): **underlying** = leading `[A-Z]+`; **right** = trailing `CE`/`PE`; **expiry** = read from the **`expiry_date` column** (`DD/MM/YY`→`YYYY-MM-DD`, D11), NOT decoded from the symbol; **strike** = middle isolated by building the expiry-derived date token (KiteConnect encoding: monthly `YY`+`MMM` e.g. `26JUN`; weekly `YY`+`M`+`DD` where M=`1`–`9` Jan–Sep / `O`,`N`,`D` Oct–Dec e.g. `26707`, `23N01`) and stripping it → digits before `CE`/`PE` = strike. **instrumentType = `Option` for all** (Option A; IndexOption NOT classified in Phase 1 → Phase-2 known-index-set backfill, **OQ-T**). **Safe-fail** on non-matching date token OR `FUT` rows (futures out of Phase-1 scope, options only) — flagged/skipped, never mis-parsed. Verified: `MAXHEALTH26JUN900PE`/`30-06-26`→900/PUT; `NIFTY2670723650PE`/`07-07-26`→23650/PUT; `BANKNIFTY23N0144500CE`→44500/CALL. Fidelity + Robinhood parsers unchanged (locked 2026-08-27). Source-of-truth docs intentionally **not yet edited**. Edits: `activeContext.md` (D10 block Zerodha + header + OQ-T + immediate-next-step), `progress.md` (OQ-T + pointer + this row), `HANDOFF.md` (§0/§7/§9/§10 + stamps). **Next: the batched source-doc edit — BrokerMapper entity + ADR-015 + all deferred changes (incl. the D9 GSI1→"by broker" coordinated sweep + FR-I2/FR-I4 rewordings).**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-09-03 | Import D9                                 | **D9 — Fidelity single-account scope + account identity ✅ LOCKED** (supersedes the 2026-08-27 paused entry). Resolved via 4 real files (3 single-account w/ `Cash Balance`, 1 multi-account w/ `Account`+`Account Number` & no `Cash Balance` — the two layouts are mutually exclusive). **Q1:** Phase 1 accepts **single-account Fidelity only**; multi-account/combined → Phase 2. **Q2:** for the 3-of-4 file types with no account column (Robinhood, Zerodha EQ, Zerodha FO, Fidelity single-account) **mint an OPAQUE deterministic per-(user,broker) `accountId`** — `acc_rh`/`acc_fid`/`acc_zer` (NOT a `userId#broker` concatenation); `broker` stays a separate attribute (not parsed from the id); find-or-create by `(userId, broker)`; `accountType` optional (Zerodha=Individual, Fidelity/RH unset); Zerodha EQ+FO unify naturally. Coupled model reframe: the real use case is **filter-by-broker**, so **GSI1/AP-11/§5.10/§10.3 reframe "by account" → "by broker"** as an all-or-nothing coordinated sweep (per-sub-account Roth/401k = Phase 2). **Q3:** D2 **UNCHANGED**, revision **CANCELLED** — Fidelity keeps `Cash Balance` key; verified stable across re-downloads (SOUN 06/29 → `41037.94` in two files). **Q4/Q7:** catalogue ALL Fidelity verbs as **LIVE Phase-1** (single-account layout can carry any of them, e.g. standalone 401k) — Description-branch LIVE: `REINVESTMENT`→BUY ("DRIP", underlying from Symbol); `DIVIDEND…FDRXX`→DIVIDEND; FDRXX sweeps→hidden (like SPAXX); `Contributions`→BUY/MutualFund (underlying from **Description**, Decision A); `Transfers`(0/0)→inert EVENT; `TRANSFERRED FROM TO BROKERAGE OPTION`→internal transfer **excluded** (Decision B). **Q5:** multi-account header registered as known-but-unsupported → **specific** rejection msg; unknown header → generic "contact support". **Q6:** reframe PRD FR-I2 → single-account Phase-1. Full single-account catalogue = 100% covered by existing decisions + these verbs. OQ-C gains HRB reinvestment evidence. Source-of-truth docs intentionally **not yet edited** — GSI1→"by broker" + FR-I2 + verb catalogue added to the deferred coordinated-sweep doc-edit list (HANDOFF §9). Edits: `activeContext.md` (D9 locked block replacing paused + header + D2 note + next-step + planned-edits), `progress.md` (OQ-C evidence + pointer + this row), `HANDOFF.md` (§0/§7/§9/§10 + stamps). **Next: D10-Zerodha** (note: both Zerodha files carry an explicit `expiry_date` column → may not need symbol-based expiry decoding). |
| 2026-08-27 | Import D14                                | **D14 — Raw-file storage (S3) ✅ LOCKED (Option B).** Phase 1 **stores a copy of every confirmed-imported broker file in S3** so Phase-2 retroactive re-import (OQ-M) + undo-an-import (PRD O6) have the raw material. Phase 1 only STORES — does NOT build the reprocess/undo engine, and nothing in Phase 1 reads the files. Rejected A/C (defer/reserve → silent permanent gap for pre-Phase-2 imports through a later-corrected mapper; discarded bytes are unrecoverable) and D (build engine now → scope creep). **WHEN: Fork B** — save at the confirm→write step (first action, before the `ImportedFile` + Trades/Cashflows writes), giving a perfect 1:1 mirror with no orphan-cleanup problem; hard-stop/cancelled files are never stored (support = "re-send it"). **Mechanism:** reuse the Layer-1 `contentHash` → save raw pre-parse bytes to S3 key `<userId>/<contentHash>` → write `ImportedFile` with new `s3Key` attr. **Six locked clarifications:** (1) SHA-256 over raw bytes, reused from Layer-1 (one hash, two uses); (2) key `<userId>/<contentHash>`, no extension, filename kept in `fileName` + S3 object metadata; (3) `s3Key` stores relative key only, bucket = app config; (4) versioning OFF + no lifecycle/expiry (keep forever; idempotent overwrite makes versions pointless, expiry would reintroduce the gap); (5) no orphan-cleanup job (self-healing via idempotent overwrite; negligible at scale); (6) backend-only least-privilege access, no download UI (Phase-1 write-and-leave PII). **Posture:** SSE-S3, Block Public Access ON, TLS-only, same region as DynamoDB. **Write order:** S3 first, then DB record. **Phase-2 forward notes:** reprocess reads via `s3Key`; undo must delete both the DB items AND the S3 object. **OQ-M → partially resolved** (storage prerequisite met; engine Phase 2). Source-of-truth docs intentionally **not yet edited** — doc footprint (`ImportedFile.s3Key`, a new raw-file-storage ADR, `techContext`/`projectbrief`/`systemPatterns` notes) bundled into the batched edit (HANDOFF §9). Edits: `activeContext.md` (D14 locked block + header progress line + OQ-M open-line + immediate-next-step), `progress.md` (OQ-M row + pointer + this row), `HANDOFF.md` (§0/§7/§9/§10 + OQ-M line + stamps). **Next: resume D9/D2-revision and D10-Zerodha.**                                                                                                                                                                                                              |
| 2026-09-03 | Import D13                                | **D13 — Reword "no manual data entry" ✅ LOCKED.** Wording fix only. Both source docs claim "no manual data entry", which overstates the flow — the locked design (L4–L5) requires **one manual step per upload**: the user reviews a read-only preview (sample + counts) and **confirms** before the write. Distinguish _typing trades by hand_ (never happens) from _the confirmation click_ (does). Approved replacements (dual-audience): **FEATURES §5.1** final sentence → _"You never type in your trades by hand — Beyond Folio does the reading for you. The only step on your side is a quick one-time confirmation of each uploaded file before it's imported."_; **PRD FR-I4** → _"…— no manual entry of trade data; the user's only input is a one-time confirmation of the uploaded file on import."_ The D4 corrected canonical import statement rides the same §5.1/FR-I4 edit as a separate labeled part (clearer provenance). Source-of-truth docs intentionally **not yet edited** — physical rewording deferred to the batched edit (HANDOFF §9). Edits: `activeContext.md` (D13 locked block + header progress line), `progress.md` (pointer + this row), `HANDOFF.md` (§0/§7/§10). **Next: D14** + resume **D9/D2-revision** and **D10-Zerodha**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-27 | Import D12                                | **D12 — Number/format cleanup ✅ LOCKED.** Scope = INPUT→STORAGE (clean canonical values into DynamoDB + for math/dedup), NOT UI/display. Mandate an **RFC-4180 CSV parser**; all cleanup on **parsed values**. Key correction: **no data-level quotes exist** — the `"($1,768.55)"` in raw bytes is CSV escaping of the thousands-comma; parsed value is `($1,768.55)` (verified via Excel LEN/CODE/SEARCH); Fidelity `Action` unquoted. Rules: **Robinhood** strip `$`, strip thousands comma, `(...)`→negative (comma/CSV-quoting only on ≥1000); **Fidelity/Zerodha** plain numbers, nothing to strip; **`S`-suffix** (RH `MRGS`: `314.3584S`) strip → keep number, meaning via `eventType=MERGER` (D3), raw in `rawAction`; **multi-line `Description`** (RH equity buy/sell rows only, carry CUSIP) handled by the parser; **Zerodha `order_id`** leading `’` strip if used. **Footer/junk-row detection:** a row is data only if it has a valid parsed date + recognized Action/Trans Code (rejects the Fidelity legal footer, blank rows, `Date downloaded…`). Corrected earlier over-generalizations (amounts/Action not data-quoted; multi-line is RH-equity-only). Source-of-truth docs untouched. Edits: `activeContext.md` (D12 block + open-line + progress line), `progress.md` (pointer + this row), `HANDOFF.md` (§0/§7/§10). **Next: D13** (reword “no manual data entry”).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-27 | Import D11                                | **D11 — Date normalization ✅ LOCKED.** Mechanical: parse each broker’s date → canonical `YYYY-MM-DD` (ADR-009 `SK`s + D2 key). Per-broker authoritative column + reading: **Robinhood `Activity Date`** (month-first, 1–2-digit M/D, 2/4-digit year), **Fidelity `Run Date`** (month-first, 2/4-digit year), **Zerodha `order_execution_time`** (ISO, unambiguous, carries time → take date portion). Direction **pinned per broker** (never guessed per value — `05/06/25`=May 6 US vs Zerodha `21/08/25`=21 Aug); **`YY→20YY`**; flexible width absorbs mixed variants within one RH/Fidelity file; malformed → **safe-fail flagged row**. Optional: keep Zerodha full `tradeDateTime` for same-day ordering (OQ-B). Clarified storage (single `YYYY-MM-DD`, already locked) vs INPUT parsing (this decision) vs DISPLAY format (**Phase-2 backlog** — user-selectable, no data impact). New **OQ-S** (verify `YY→20YY`; confirm RH/Fidelity have no intraday time). Source-of-truth docs untouched. Edits: `activeContext.md` (D11 block + open-line + progress line), `progress.md` (OQ-S + Phase-2 backlog + pointer + this row), `HANDOFF.md` (§0/§7/§8/§10). **Next: D12** (number/format cleanup).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-27 | Import D10 (partial)                      | **D10 — Option `optionDetails` parse rules 🟡 PARTIALLY LOCKED (Fidelity + Robinhood; Zerodha deferred).** Confirmed the per-broker parsers behind D7’s shared `extract`, each → `{underlying, right, strike, expiry}`. **Fidelity** (on `Symbol`): `^-([A-Z]+)(\d{2})(\d{2})(\d{2})([CP])([0-9.]+)$` — `20YY-MM-DD`, C/P, decimal strikes (verified 96 distinct codes). **Robinhood** (on `Description`): `^(?:Option (?:Expiration                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Assignment) for )?([A-Z.]+) (\d{1,2})/(\d{1,2})/(\d{4}) (Call | Put) \$([0-9.]+)$`— optional prefix handles`OEXP`/`OASGN`(D3 events); Robinhood has **no Action column** (classify from`Trans Code`, extract from `Description`). **Also corrected the D7 line-217 note** (was over-generalized to “Symbol code / not from Action”) to a broker-specific source + explicit “Robinhood has no Action column”, and added **D7↔D10 cross-references** to prevent implementation drift. Failure = safe-fail (option imports, `optionDetails`flagged). Verify-flag: Fidelity`YY→20YY`. **Zerodha DEFERRED** (monthly `MAXHEALTH26JUN900PE`vs weekly`NIFTY2670723650PE`expiry encoding — pending user info). Source-of-truth docs untouched. Edits:`activeContext.md`(D10 block + D7 note correction + open-line + progress line),`progress.md`(pointer + this row),`HANDOFF.md` (§0/§7/§10). **Next: D11.** |
| 2026-08-27 | Import D9 (paused)                        | **D9 — Multi-account Fidelity identification ⏸️ PAUSED (in-progress).** A real multi-account export (`fidelity.csv`) ANSWERED the core question: it carries explicit **`Account`** (display/type) + **`Account Number`** columns → **`accountId` = `Account Number`** (direct), find-or-create by `(broker, accountNumber)`; FR-I2 "multiple accounts in one file" confirmed. **Paused** because it cascades: (1) **D2 revision** — the multi-account layout has **NO `Cash Balance`** (D2’s locked Fidelity fingerprint column), so that key breaks; verified a Cash-Balance-free natural key `Date+Account#+Action+Symbol+Description+Qty+Price+Amount+Settlement` = **0 collisions on 104 rows** (`Description` load-bearing for 401k Transfers), but this file lacks D2’s original hard case (identical same-day same-contract trades) → recommended **natural key + occurrence-index `#N`** (Robinhood mechanism), NOT Cash Balance — decision OPEN; (2) new transaction types **401k `Contributions`/`Transfers`**; (3) new verbs **`REINVESTMENT`** (DRIP, ties OQ-C), **`TRANSFERRED FROM TO BROKERAGE OPTION`**; (4) second money-market fund **`FDRXX`**; (5) **two Fidelity header layouts** (multi-account has account cols + no Cash Balance; single-account the reverse → likely two mappers); (6) **401k fund is in `Description`, `Symbol` empty** (D7 extract must branch); (7) multi-line **footer** to ignore. Corrected two of my own misreads mid-analysis (fund is in `Description` not `Symbol`; only `Transfers` rows are Qty/Amount 0, `Contributions` are non-zero). To resume: pick fingerprint style, then lock D9 + D2-revision + new-types amendment together. No source-of-truth edits. Edits: `activeContext.md` (D9 paused block + open-line + progress line), `progress.md` (pointer + this row), `HANDOFF.md` (§0/§7/§10). **Next: D10** (option parse rules).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-27 | Import D5-revision (richer Fidelity file) | **D1/D5/D7 amended after a second, richer Fidelity export (`fidelity-2025.csv`).** New pattern found: **`Electronic Funds Transfer Received/Paid`** rows (13) = real external deposits/withdrawals — the exact rows D5 had assumed were absent. **D1:** added `Electronic Funds Transfer Received→DEPOSIT`, `Paid→WITHDRAWAL` (both `includeInXIRR=true`, Cashflow-only, empty Symbol). **D5 (revised):** premise corrected — Fidelity XIRR now funded from these transfer rows; SPAXX sweeps unchanged (`includeInXIRR=false`/hidden); the "external contributions not included" notice narrowed to degraded-mode only (uploads with no transfer rows); `Cash Balance` reconciliation demoted from primary Phase-2 plan to a fallback. **D7:** added two `classify` word-set rules `{ELECTRONIC,FUNDS,TRANSFER,RECEIVED}`→DEPOSIT and `{...,PAID}`→WITHDRAWAL. **Verified:** classifier now **0 ties / 0 unmapped across BOTH files = 588 Action rows** (319 + 269). Only genuinely-new pattern in the richer file was the EFT rows; all others (SPAXX, OPENING/CLOSING options, RSU-buy, ASSIGNED/EXPIRED events, AGENCY CROSS/VSP noisy prefixes, ROYALTY, DIVIDEND) were already covered. **OQ-K** → largely resolved; residual verification folded into OQ-Q. Source-of-truth docs still **not yet edited**. Edits: `activeContext.md` (D1/D4-table/D5/D7), `progress.md` (OQ-K + this row). **Next: D9** (multi-account Fidelity identification — note: richer file still has NO account column, supporting "one Fidelity upload = one account").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-27 | Import D8                                 | **D8 — Capture security identifier (CUSIP/ISIN) ✅ LOCKED (Option 2).** Blocker (MEDIUM): each file carries a security id we weren’t storing — RH **CUSIP** (buried in `Description`, equities only), Zerodha **ISIN** (clean column, EQ only), **Fidelity none**; no Phase-1 feature reads it (value is Phase-2). Decision: add **optional `securityId`+`securityIdType`** on Trade (one neutral pair, refining HANDOFF §9’s cusip/isin placeholder); populate where present (Zerodha `isin`→`direct`; RH `CUSIP:\s*(\w+)` via the D7 `Description` extract); **absent/never fabricated** on Fidelity/options/cash; **NOT part of D2 dedup identity**; stored-but-unused in Phase 1. Cleared the user’s doubt: a merger **changes** the CUSIP — the _change_ is the signal, not constancy (real SIRI `MRGS`: same ticker, `829933100` OUT → `82968B103` IN); `securityId` helps Phase-2 disambiguate reorg legs, match a reorg to the historical position, and link a security across brokers where the id exists. Rejected: skip (permanent loss under forward-only L9), Zerodha-ISIN-only (asymmetric, no US), raw-only (unqueryable). New **OQ-R** (verify RH CUSIP format + Zerodha ISIN reliability; Fidelity-has-no-id caveat). Source-of-truth docs intentionally **not yet edited**. Edits: `activeContext.md` (D8 block + open-line + progress line), `progress.md` (OQ-R + pointer + this row), `HANDOFF.md` (§0/§7/§8/§10). **Next: D9** (multi-account Fidelity identification).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-27 | Import D7                                 | **D7 — `columnMapping` structure ✅ LOCKED.** Blocker (MEDIUM, structural keystone): the BrokerMapper needs a concrete way to turn a raw row into our attributes, but broker columns don’t map 1-to-1 — some renames, some clean codes, and Fidelity `Action` is free text needing classification. Decision: **four entry kinds** — (1) `direct` (copy col→attr + D11/D12 cleanup); (2) `lookup` (finite code columns e.g. RH `Trans Code` → fixed fields, = D1/D4 catalogue as data); (3) `classify` (free-text Action → category via a **word-set matcher**: tokenize on non-letters → whole-word set, rule matches if all its words present, **largest matched set wins**, exact-tie/no-match → `unmapped`; **glue-free** — keys only on verbs/CALL/PUT/SPAXX, never AS OF/INTO/TRANSACTION; validated **0 ties/0 unmapped on all 319 Fidelity Action rows**); (4) shared `extract` that parses the **structured Symbol code** (Fidelity `-AAPL260918P300`, Zerodha `NIFTY2670723650PE`) into option details. **Underlying/rawSymbol comes from the `Symbol` column via `direct` for ALL types — NOT parsed from Action; classify is category-only** (RSU-tax has empty Symbol = correct; fallback parses trailing `(XXX)` from Action only if a real export leaves Symbol blank). ~24 Fidelity word-set rule variants cover 14 patterns (BUY/SELL share via side-from-verb). Rejected: regex-rule engine / keyword-scoring engine as primary (more machinery than justified for 5–6 fixed brokers + silent-misroute risk); admin-authored regex (unsafe/over-engineered). Per-broker: Fidelity=direct+classify(Action)+extract(Symbol); Robinhood=direct+lookup(Trans Code)+extract; Zerodha=direct+structured symbol parser. New **OQ-Q** (verify word-sets + Symbol reliability vs a real export). Source-of-truth docs intentionally **not yet edited**. Edits: `activeContext.md` (D7 block + open-line + progress line), `progress.md` (OQ-Q + pointer + this row), `HANDOFF.md` (§0/§7/§8/§10). **Next: D8** (capture cusip/isin?).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-27 | Import D6                                 | **D6 — Stock-vs-ETF classification ✅ LOCKED (Option 4a, modified).** Blocker (MEDIUM): every US equity Trade needs an `instrumentType`, but neither US file has a type column — only inconsistent free-text (many ETFs say "ETF"; company names give no positive "stock" signal). Not correctness-blocking (XIRR/dedup unaffected; FR-H5 charts Stock & ETF identically) — a labelling decision. Decision: **default `instrumentType=Stock`; upgrade to `ETF` only when the Description contains an unambiguous "ETF" (/"EXCHANGE TRADED FUND") token** — conservatively (no FUND/TRUST/SHARES, which cause false positives like "MESABI TRUST"). Never emits a wrong ETF; may under-label some ETFs as Stock (harmless). Rejected: collapse-to-`Equity` (user wants both labels), broad keyword heuristic (false positives), external lookup now (network dep + ties OQ-E, too heavy for a cosmetic label). OQ-L resolved for Phase 1; Phase-2 = authoritative symbol lookup (likely OQ-E provider) backfills true types. Source-of-truth docs intentionally **not yet edited**. Edits: `activeContext.md` (D6 block + open-line + OQ-L + progress line), `progress.md` (OQ-L + pointer + this row), `HANDOFF.md` (§0/§7/§8/§10). **Next: D7** (`columnMapping` structure).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-27 | Import D5                                 | **D5 — Fidelity external cashflows + SPAXX core sweeps ✅ LOCKED (Option D).** Blocker: XIRR needs external deposits/withdrawals, but the Fidelity tradebook contains none — only internal SPAXX money-market sweeps (which cannot distinguish new outside money from internal reshuffling). A missing-data-source gap (High, OQ-K) that would leave the headline XIRR feature wrong for a core broker. Decision: (1) SPAXX `PURCHASE/REDEMPTION` sweeps stored `includeInXIRR=false` & hidden from Trade History (SPAXX interest stays DIVIDEND/counted); (2) Fidelity external deposits/withdrawals absent from the file and **NOT fabricated** in Phase 1 (no manual entry, D13); (3) Fidelity XIRR shown with an "external contributions not included" notice (OQ-I precedent); (4) Phase-2 direction = **`Cash Balance` reconciliation** (walk rows in date order, plug unexplained balance deltas as inferred deposits/withdrawals), deferred because fragile (sweep timing, same-day ordering, rounding, multi-account split per D9) and pairs with the Phase-2 position tracker. Rejected: B manual entry (violates D13), C-in-Phase-1 (too fragile now), external statement/API (out of scope). OQ-K raised Medium→High and resolved for Phase 1. Source-of-truth docs intentionally **not yet edited** (batched later per HANDOFF §9). Edits: `activeContext.md` (D5 block + open-line + progress line), `progress.md` (OQ-K + pointer + this row), `HANDOFF.md` (§7/§8/§10). **Next: D6** (Stock-vs-ETF classification → OQ-L).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-27 | Import D4                                 | **D4 — `cashflowType` enum + XIRR membership ✅ LOCKED.** Blocker: D1 expanded categories but the persisted `cashflowType` enum (§7.5, 6 values) never caught up, and per-category XIRR membership for the income tail was undecided — blocking the normalizer's INCOME/ROYALTY/ADJUSTMENT branch, XIRR correctness, and the §7 doc edit. Decisions: (1) **keep `includeInXIRR` as an explicit per-Cashflow-ONLY boolean** (flexible/flippable per OQ-N; justified because we deliberately store Cashflows excluded from XIRR — SPAXX sweeps, RSU-tax); (2) **enum grows 6→10**: `BUY, SELL, DEPOSIT, WITHDRAWAL, DIVIDEND, INTEREST, INCOME, FEE, ROYALTY, ADJUSTMENT` (INTEREST its own type; INCOME = catch-all; no CAPITAL_GAINS type; exact broker code preserved in `rawAction`); (3) **per-code mapping**: RH `INT`→INTEREST, `SLIP`/`GDBP`→INCOME, `MDIV`/`LCAP`/`SCAP`/`CDIV`→DIVIDEND, Fidelity `ROYALTY TR PYMT`→ROYALTY (all `includeInXIRR=true`), RSU-tax `JOURNALED RSU`→ADJUSTMENT & SPAXX sweeps → `includeInXIRR=false`. Also restated ADR-011 row→item mapping (buy/sell=2 items; cash-only=1 Cashflow; EVENT=1 Trade) + corrected canonical import statement for the batch edit. **D3 tidy-up:** EVENT Trades are excluded from XIRR **structurally** (no Cashflow + `eventType`), NOT via `includeInXIRR` (which is Cashflow-only). New **OQ-P** (income-tail mappings to verify vs real files). Source-of-truth docs intentionally **not yet edited** (batched later per HANDOFF §9). Edits: `activeContext.md` (D4 block + D3 tidy-up + progress line), `progress.md` (OQ-P + pointer + this row), `HANDOFF.md` (§7/§8/§9/§10 + D3 tidy-up). **Next: D5** (Fidelity external cashflows + SPAXX sweeps → OQ-K).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-27 | Import D3                                 | **D3 — Non-trade position events ✅ LOCKED (hybrid, Approach C).** The EVENT bucket from D1 (Fidelity `ASSIGNED/EXPIRED`; Robinhood `OASGN/OEXP/MRGS/SPL/REC`; Zerodha none) needed a normalizer rule — it's a real modeling fork (an EVENT fits neither the Trade buy/sell shape nor the Cashflow shape), blocking the normalizer's EVENT branch, the preview counts, and the Trade schema. Decision: **stamp an `eventType` enum on the Trade item at import** (`ASSIGNMENT, EXERCISE, EXPIRATION, MERGER, SPLIT, RECLASS`, extensible) from D1's classification; EVENT rows are **inert in Phase 1** (`includeInXIRR=false`, not netted into holdings, `rawAction` preserved) so the paired real-cash BUY/SELL rows carry all impact (no double-count, no data loss); **shown in Trade History as non-cash informational rows** (user choice — refines D1 rule #2; distinct from hidden SPAXX sweeps); **interpretation deferred to the Phase-2 execution-ordered position tracker** (split ratios, merger symbol-swaps, assignment/expiration close — ties to OQ-B). Rejected: SKIP (data loss), raw-only (Phase-2 re-parses free-text), interpret-now (drags Phase-2 engine forward for zero Phase-1 benefit). New **OQ-O** (corporate-action interpretation needs data the files don't fully give — `SPL` top-up qty not ratio; `MRGS` in/out with `S`-suffix). Source-of-truth docs intentionally **not yet edited** (batched later per HANDOFF §9). Edits: `activeContext.md` (D3 locked block + progress line), `progress.md` (OQ-O + in-progress pointer + this row), `HANDOFF.md` (§7/§8/§9/§10). **Next: D4** (`cashflowType` enum scope + income rows).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-27 | Session 13                                | **Trade Price Chart with Transaction Markers added to Phase 1 (FR-H5 / §5.12 / ADR-014 / OQ-E).** On an **equity** trade detail (Stock/ETF only) show that ticker's historical price line with a **circle marker per user buy/sell** (green=buy, red=sell; tooltip type·date·price·qty). **4 locked decisions:** (1) historical series = new **ephemeral `PRICEHIST#<canonicalSymbol>#<exchange>` cache** (§9.6, price-cache sibling, ADR-008 family), **lazy fetch + 1-day freshness** (reuse if `fetchedDate`=today, else re-fetch once); cost analysis showed this ~halves API calls & kills same-day duplicates for ~pennies/mo vs fetch-every-open. (2) Chart scoped to **opened trade's ticker + market/currency** — markers span all the user's accounts on that market (reuse **AP-30/GSI3** + app-side currency filter); dual-listed tickers (INFY INR vs USD ADR) get one chart per market so a rupee marker never lands on a dollar line. (3) **Stock+ETF only**, else fallback message. (4) **Phase 1 = factual buy/sell markers only, NO P/L** — realized P/L on sells (FIFO vs avg-cost differ materially, neither tax-grade), triangle markers, and profit/loss sell-colouring all **deferred to Phase 2**. New model bits: **AP-31/AP-32** (§4.9), **§9.6** cache, **§10.7** chart mechanism, **ADR-014**, **OQ-E** (§13.1.7). **No new durable item type.** Non-goals **narrowed not removed** (charts→analytics dashboards; P&L→tax-grade). Edits: `FEATURES.md` (§5.12/§6/§7), `PRD.md` (FR-H5 §7.4 [Should], §4.2/§9/§10/§11), `DYNAMODB_DATA_MODEL.md` (§1.5, §4.9, ADR-014, §9.6, §10.7, §12.1, §13.1.7), memory bank. Recommended next: PRD sign-off, then TRD (settles OQ-E provider/range + OQ-F session).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

| 2026-07-25 | Session 12 | **Auth pivot: Amazon Cognito → self-managed OAuth 2.0 (Google Phase 1, Zerodha Phase 2).** Deciding factor: Zerodha (Kite Connect) is a bespoke broker login, not a standards-compliant OIDC IdP Cognito federates cleanly — routing Google through Cognito while bolting Zerodha on separately would fragment auth. Decision: **run OAuth/OIDC ourselves** — verify the provider's token against its JWKS (Google), mint our **own internal `userId`**, and map external identities via a new **AuthIdentity** lookup item (`AUTH#<provider>#<sub>` → `userId`, written idempotently; a non-user-rooted system item like `GLOBAL#SYMBOLMAP`). `role` (`user`/`admin`) is **app-managed** on the `User` item; no passwords, no email-verification/password-reset (the provider owns identity); MFA is the provider's concern; **first-login provisioning** (resolves **OQ-D**). We issue our **own session** (mechanism deferred = **OQ-F**). **Phase-2 account-linking** (Google↔Zerodha as one account) = new **OQ-G**. Edits: `DYNAMODB_DATA_MODEL.md` (ADR-013 rewritten, §7.1 User, new §7.12 AuthIdentity, §4.1 AP-2b/AP-2c, §6.5, §7.11, §11 examples, §12.1/§12.2, §13.1.4 OQ-D resolved + §13.1.5 OQ-F + §13.1.6 OQ-G, §13.2 A-9); `FEATURES.md` (§5.11 + §7 OQ-F/OQ-G); `PRD.md` (§6, §7.1 FR-A1…A6, §9, §10, §11, §12); memory bank (systemPatterns, techContext, productContext, projectbrief, this file, activeContext). Cognito is now the **rejected alternative** in ADR-013. FR-H5 chart / historical price / OQ-E remain deferred; TRD not yet written. |

| 2026-07-16 | Session 11 | **Authentication added as a first-class feature → Amazon Cognito.** Signup/login was never modeled. After discussion (self-managed vs. managed; Cognito is ~free through 1,000 MAUs, so no cost advantage to self-managing), decided: **auth delegated to Amazon Cognito** — `userId` = Cognito `sub`, JWT verified per request via JWKS, stateless backend (no sessions table), no passwords stored; `User` item is a thin profile mirror gaining a `role` (`user`/`admin`, from Cognito group claim). Email verification + password reset **in** Phase 1; **MFA deferred** to Phase 2. Edits: `DYNAMODB_DATA_MODEL.md` (§4.1 AP-2a/AP-2b, ADR-013, §7.1 `role` + callouts, §12.1/§12.2, §13.1.4 OQ-D, §13.2 A-9); `FEATURES.md` (new §5.11); `techContext.md` (auth → Decided + Cognito dependency + OQ-D + A-9); `systemPatterns.md` (invariant #11, ADR-013, OQ-D). **OQ-D** (profile provisioning: Post-Confirmation Lambda vs. lazy get-or-create) deferred as an implementation detail — identical `User` item either way, so no schema/flow void. |

| 2026-07-10 | Session 10 | **Memory-bank rewrites → DynamoDB.** Rewrote `systemPatterns.md` (DynamoDB single-table design: two tables, key structure, 10 item types, 2 GSIs, invariants, 12 ADRs, denormalizations, grouped access patterns, OQ-A/B/C) _(point-in-time counts; the model later grew to 3 GSIs / 14 ADRs / 13 item types after Sessions 12–13)_ and `techContext.md` (engine, mechanisms, external deps, security, assumptions A-1…A-8, AWS-native deployment) from `DYNAMODB_DATA_MODEL.md`. Updated `README.md` banners + Reading Order/SoT rows, and `activeContext.md` current focus + session summary. All ⚠️ SUPERSEDED memory-bank files are now retired — the memory bank is fully consistent with the DynamoDB model. Recommended next: implementation planning. |
| 2026-05-20 | Session 1 | Read DMA cover-to-cover; confirmed polyglot persistence analysis scope; created memory bank to persist context. |
| 2026-05-28 | Session 2 | Locked context for polyglot analysis: output → new `DB_ALTERNATIVES_ANALYSIS.md`, alternatives → DynamoDB/Redis/MongoDB/S3, cloud → AWS, budget → ~$100/mo, scale → 1K-user stretch, workflow → table-by-table. About to begin per-table profiles. |
| 2026-06-17 | Session 3 | **DB pivot: PostgreSQL → DynamoDB (replacement).** Decided to design a fresh DynamoDB-native model derived **only from `FEATURES.md`**. Agreed architecture (pragmatic mix: main single-table + price-cache table), 13-section skeleton with an ADR/rationale section, and an 8-batch append-only workflow into `DYNAMODB_DATA_MODEL.md` with a per-batch review gate. Superseded the polyglot analysis. Updated memory bank. Next: write Batch 1. |
| 2026-06-22 | Session 4 | **Batches 1 & 2 written & reviewed.** Wrote §1–§4 of `DYNAMODB_DATA_MODEL.md`. Reworked §2 principles into a balanced `Do:`/`Don't:` colon-label format + adopted a "no hard line wraps" convention. Clarified the NoSQL "entities ≠ tables" idea (11 concepts → 2 tables) before §3. Confirmed two DynamoDB-era modeling decisions, recorded as ADRs in the data-model doc (single source of truth, not duplicated as D-IDs): **ADR-011** (Trade vs Cashflow are separate concepts) and **ADR-012** (SymbolMapping is global/admin-owned — the one exception to per-user rooting). §4 catalogs access patterns AP-1…AP-29. Next: write Batch 3 (§5 ADRs, formalizing ADR-001…ADR-012). |
| 2026-06-23 | Session 5 | **Batch 3 (§5 ADRs) written & reviewed.** Formalized **ADR-001…ADR-012** (001–010 align 1:1 with §2 principles; 011/012 authored fresh). **Dual-audience readability pivot:** user found §5 too jargon-heavy and set the standard that the doc must be readable by **both a developer and a business stakeholder**. Solution = **layered ADRs** — each opens with an **"In plain terms"** jargon-free line, prose reworded to lead with the idea then the mechanism, and unavoidable terms glossed on first use; added a "How to read an ADR" note. **New writing convention** to apply to §6–§12 as written, with a §1–§4 jargon sweep in the Batch 8 pass. ADRs remain single-source-of-truth in the data-model doc (no new D-IDs). Next: write Batch 4 (§6 Table Design Overview). |
| 2026-06-23 | Session 6 | **Batches 4 & 5 written & reviewed.** **Batch 4 (§6 Table Design Overview)** — bird's-eye physical layout: the two tables at a glance (durable per-user `BeyondFolio` vs ephemeral global price cache), composite `PK`/`SK` structure, item-type overloading + `entityType`, the global SymbolMapping partition (the one exception), and a forward map of what lives where + a GSI preview. **Batch 5 (§7 Item Definitions · §8 GSIs)** — concrete `PK`/`SK` shapes + attribute tables (with a "Denorm?" column) for all 10 item types, a §7.11 summary table, and exactly **two GSIs** (GSI1 trades-by-account → AP-11; GSI2 journal-entries-by-tag → AP-21), both user-scoped; §8.3 proves every other read is served by the base table (no over-indexing). **Four key-shape decisions confirmed by user before writing:** (1) tag identity = name (`TAG#<tagName>`, idempotent); (2) journal↔tag = link items + `tagNames` denormalized onto the entry + GSI2 for reverse; (3) current holdings stays **derived** from trades (no Holdings item — out of scope §1.5); (4) cashflow **currency-in-the-sort-key** (`CASHFLOW#<currency>#<date>#<id>`) serves AP-13 from base table, no cashflow GSI. New standing preference: **do not auto-open the doc** on completion (user opens manually). Dual-audience style applied throughout. Next: write Batch 6 (§9 Price Cache · §10 Tricky Requirements). |
| 2026-06-25 | Session 7 | **Batch 6 (§9 Price Cache · §10 Tricky Requirements) written, awaiting review.** **Two open modeling choices confirmed by user before writing:** (1) **§9 price-cache key** = single PK, no SK: `PRICE#<canonicalSymbol>#<exchange>` (canonical/post-resolution symbol; exchange folded in to disambiguate same-ticker-different-market); tiny projection (`price`, `currency`, `asOf`, `expiresAt` TTL attr in epoch seconds); **treat expired-but-not-yet-deleted as missing** read rule; cache write is a plain **last-write-wins overwrite** (not conditional). (2) **§10 cashflow `amount` sign convention** (resolves §7.5 deferral) = investor's perspective — `DEPOSIT`/`BUY`/`FEE` **negative**, `WITHDRAWAL`/`SELL`/`DIVIDEND` **positive**, derived terminal holdings valuation **positive**. **§9** covers purpose/lifecycle, key+attributes, TTL auto-expiry, the expired-as-missing rule, and serving AP-28/AP-29. **§10** = one subsection per hard requirement: two-layer dedup (ADR-005/011), XIRR assembly (per-currency timeline AP-13 + **holdings derived from trades** AP-14, ADR-007/009/001, incl. sign table), multi-account/multi-currency isolation, journal↔tags many-to-many + evaluation→atomic scorecard counter (ADR-004/006), corporate-action symbol resolution (ADR-010/012), and a §10.6 limitations table (Robinhood same-day transfers, Zerodha F&O open/close, DRIP) all feeding §13. Dual-audience style maintained. Next: write Batch 7 (§11 Example Items · §12 AP→Implementation Mapping). |
| 2026-06-26 | Session 8 | **Batch 6 confirmed approved**, then **Batch 7 (§11 Example Items · §12 AP→Implementation Mapping) written, awaiting review.** User expanded §11 scope: two users, all three brokers, broad instrument coverage, and limitation examples shown as records. **§11** = one coherent worked scenario — `u_alex` (US: Robinhood USD + Fidelity import across 3 accounts Individual/Roth/401k) and `u_priya` (India: Zerodha INR). Covers User profiles, 5 Accounts, ImportedFiles (incl. multi-file Zerodha upload), Trades spanning Stock(FB→META)/ETF/fractional/US Option/MutualFund/401k/Indian equity/NIFTY IndexOption, Cashflows (all 6 types, both currencies, §10.2 signs), 2 JournalEntries (multiple-entries + multiple-tags, WIN/LOSS), 3 Tags, 3 JournalTagLinks (with GSI2 keys), TagScorecards matching evaluations, global SymbolMapping, 3 PriceCache items (one expired), a §11.13 partition view (both users), and §11.14 limitation examples as records (same-day Robinhood collision, Zerodha F&O open/close, DRIP). **§12** = full AP-1…AP-29 implementation table (operation · table/index · key condition · ADRs) + §12.2 index-utilization read-back proving no orphan indexes, closing the §4.9/§8.3 loop. Dual-audience style maintained; internal consistency verified. Next: write Batch 8 (§13 Open Questions & Assumptions + full-document consistency pass) — final batch. |
| 2026-06-26 | Session 9 | **Batch 8 (FINAL) written — `DYNAMODB_DATA_MODEL.md` is COMPLETE.** Batch 7 confirmed approved (§11/§12 → reviewed). **§13 Open Questions & Assumptions** appended in dual-audience style with a "no new decision IDs" note (doc ADRs + §13 are single source of truth; relational-era OQ-006/OQ-007 are reference-only). **§13.1** formalizes the three §10.6/§11.14 items as structured blocks (_what it is · where it bites · Phase-1 disposition · proposed Phase-2 direction_): **OQ-A** identical same-day transfers collapsing on one deterministic `cashflowId` (ADR-005); **OQ-B** Zerodha F&O open/close ambiguity → derived holdings/XIRR (Phase-2 = execution-time-ordered position tracker); **OQ-C** DRIP record pattern varies by broker (verify vs real files). **§13.2** = 8-row Assumptions table (Assumption · Relied on at · Anchored by): tag-name stability, derived holdings, per-currency XIRR, canonical symbol resolution, cache window as app config, illustrative ID formats, exchange known/derivable, per-user isolation sufficient. **Full consistency pass:** fixed the stray blank lines in the Section Index table (now one clean 13-row table), flipped all 13 rows to ✅, added a first-use `denormalize` gloss in §2.4, verified cross-refs/key-shapes/sign-convention/canonical-symbol usage, confirmed no references to retired relational docs. Doc **not** auto-opened (standing preference). **Recommended next:** full DynamoDB rewrites of `techContext.md` and `systemPatterns.md` (both still ⚠️ SUPERSEDED). |

---

## Known Limitations Accepted for Phase 1

| ID    | Description                                                 | Severity |
| ----- | ----------------------------------------------------------- | -------- |
| D-032 | Robinhood ACH same-day same-amount hash collision           | Low      |
| D-025 | NULL exchange concurrent-insert race for money market funds | Low      |
| D-010 | Plaintext `account_number` storage                          | Medium   |

---

## Phase 2+ Backlog (Out of Phase 1 Scope)

- Holdings / Portfolio View
- Realized / Unrealized P&L
- Analytics / Dashboard tables
- Currency conversion / unified multi-currency XIRR — a **combined base-currency XIRR** (user-toggled base: USD or INR), computed by converting **each cashflow at its own date's FX rate** then running a single XIRR; needs a historical + current FX-rate source and supersedes ADR-007's "no conversion" for that view. Per-currency XIRR stays the primary, always-correct figure. _(Decision 2026-07-28: kept Phase 2; combining two finished rates is mathematically invalid — must convert cashflows, then compute once.)_
- Audit log / soft deletes
- Custom user-defined instruments
- Full `orders` table
- `account_number` encryption-at-rest
- PostgreSQL Row-Level Security policies
- **User-selectable display date format** (UI/Settings) — storage stays canonical `YYYY-MM-DD` (ADR-009); render in any locale/format. Deferred from D11.
